<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/71f8f10969.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-md navbar-dark bg-info">
        <div class="container">
          <a class="navbar-brand p-3" href="/">
            <h2>ながのCAKE</h2>
          </a>

          <% if customer_signed_in? %>
            <p>ようこそ、<%= current_customer.last_name %>さん！</p>
          <% end %>

          <div class="gnav">
            <div class="row">
              <ul class="navbar-nav navbar-expand ml-auto py-2">
                <% if customer_signed_in? %>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "マイページ", "customer/:id", class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "商品一覧", items_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "カート", cart_items_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class: 'btn btn-light text-info' %>
                  </li>
                <% elsif admin_signed_in? %>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "商品一覧", admin_items_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "会員一覧", admin_customers_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "注文履歴一覧", admin_root_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-4 mr-2">
                    <%= link_to "ジャンル一覧", admin_genres_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'btn btn-light text-info' %>
                  </li>
                <% else %>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "About", homes_about_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "商品一覧", items_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item mr-lg-4 mr-2">
                    <%= link_to "新規登録", new_customer_registration_path, class: 'btn btn-light text-info' %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "ログイン", customer_session_path, class: 'btn btn-light text-info' %>
                  </li>
                <% end %>
              </ul>
            </div>
            <% if admin_signed_in? %>
              <%= form_with url: admin_items_path, method: :get do |f| %>
                <div class="row d-flex my-2 bg-light col-lg-9 ml-md-auto ml-1">
                  <div class="col-11 m-0 p-0">
                    <%= f.text_field :name, placeholder: "Search", class: "flex-grow-1 form-control bg-light border-light" %>
                  </div>
                  <div class="col-1 m-0 p-0">
                    <%= button_tag type: "submit", class: "btn btn-default" do %>
                      <i class="fa fa-search fa-rotate-90 text-info"></i>
                    <% end %>
                  </div>
                </div>
              <% end %>
            <% else %>
              <%= form_with url: items_path, method: :get do |f| %>
                <div class="row d-flex my-2 bg-light col-lg-9 ml-md-auto ml-1">
                  <div class="col-11 m-0 p-0">
                    <%= f.text_field :name, placeholder: "Search", class: "flex-grow-1 form-control bg-light border-light" %>
                  </div>
                  <div class="col-1 m-0 p-0">
                    <%= button_tag type: "submit", class: "btn btn-default" do %>
                      <i class="fa fa-search fa-rotate-90 text-info"></i>
                    <% end %>
                  </div>
                </div>
              <% end %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main class="mb-auto">
      <%= yield %>
    </main>

    <footer class="bg-info p-5"></footer>
  </body>
</html>